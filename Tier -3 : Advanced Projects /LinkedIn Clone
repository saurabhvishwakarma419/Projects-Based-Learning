import React, { useState } from 'react';
import { Home, Users, Briefcase, MessageSquare, Bell, Search, ThumbsUp, MessageCircle, Share2, Send, Image, Video, Calendar, MoreHorizontal } from 'lucide-react';

export default function LinkedInClone() {
  const [activeTab, setActiveTab] = useState('home');
  const [posts, setPosts] = useState([
    {
      id: 1,
      author: 'Sarah Johnson',
      title: 'Senior Product Manager at Tech Corp',
      time: '2h',
      content: 'Excited to share that our team just launched a new feature that will help millions of users! Grateful for the amazing team effort. 🚀',
      image: null,
      likes: 124,
      comments: 18,
      shares: 5
    },
    {
      id: 2,
      author: 'Michael Chen',
      title: 'Software Engineer | AI Enthusiast',
      time: '5h',
      content: 'Just completed a deep dive into React performance optimization. Here are my top 5 takeaways:\n\n1. Use React.memo wisely\n2. Implement code splitting\n3. Optimize re-renders with useMemo\n4. Lazy load components\n5. Monitor with React DevTools\n\nWhat are your favorite optimization techniques?',
      image: null,
      likes: 289,
      comments: 45,
      shares: 23
    }
  ]);
  const [postText, setPostText] = useState('');

  const handlePost = () => {
    if (postText.trim()) {
      const newPost = {
        id: posts.length + 1,
        author: 'You',
        title: 'Professional Title',
        time: 'Just now',
        content: postText,
        image: null,
        likes: 0,
        comments: 0,
        shares: 0
      };
      setPosts([newPost, ...posts]);
      setPostText('');
    }
  };

  const NavBar = () => (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50">
      <div className="max-w-6xl mx-auto px-4">
        <div className="flex items-center justify-between h-14">
          <div className="flex items-center gap-2">
            <div className="w-9 h-9 bg-blue-600 rounded flex items-center justify-center">
              <span className="text-white font-bold text-xl">in</span>
            </div>
            <div className="relative">
              <Search className="absolute left-2 top-2 w-4 h-4 text-gray-500" />
              <input
                type="text"
                placeholder="Search"
                className="w-64 pl-9 pr-3 py-1.5 bg-blue-50 rounded text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
              />
            </div>
          </div>
          
          <div className="flex gap-8">
            {[
              { icon: Home, label: 'Home', id: 'home' },
              { icon: Users, label: 'Network', id: 'network' },
              { icon: Briefcase, label: 'Jobs', id: 'jobs' },
              { icon: MessageSquare, label: 'Messaging', id: 'messaging' },
              { icon: Bell, label: 'Notifications', id: 'notifications' }
            ].map(item => (
              <button
                key={item.id}
                onClick={() => setActiveTab(item.id)}
                className={`flex flex-col items-center gap-1 ${
                  activeTab === item.id ? 'text-gray-900' : 'text-gray-600 hover:text-gray-900'
                }`}
              >
                <item.icon className="w-6 h-6" />
                <span className="text-xs">{item.label}</span>
              </button>
            ))}
          </div>
        </div>
      </div>
    </nav>
  );

  const ProfileCard = () => (
    <div className="bg-white rounded-lg border border-gray-200 overflow-hidden">
      <div className="h-16 bg-gradient-to-r from-blue-500 to-blue-600"></div>
      <div className="px-4 pb-4">
        <div className="flex flex-col items-center -mt-8">
          <div className="w-16 h-16 rounded-full bg-gray-300 border-4 border-white"></div>
          <h3 className="mt-2 font-semibold text-sm">Your Name</h3>
          <p className="text-xs text-gray-600 text-center mt-1">Professional Title | Industry</p>
        </div>
        <div className="mt-4 pt-4 border-t border-gray-200">
          <div className="flex justify-between text-xs">
            <span className="text-gray-600">Profile viewers</span>
            <span className="text-blue-600 font-semibold">142</span>
          </div>
          <div className="flex justify-between text-xs mt-2">
            <span className="text-gray-600">Post impressions</span>
            <span className="text-blue-600 font-semibold">1,847</span>
          </div>
        </div>
      </div>
    </div>
  );

  const CreatePost = () => (
    <div className="bg-white rounded-lg border border-gray-200 p-4">
      <div className="flex gap-3">
        <div className="w-12 h-12 rounded-full bg-gray-300 flex-shrink-0"></div>
        <textarea
          value={postText}
          onChange={(e) => setPostText(e.target.value)}
          placeholder="Start a post..."
          className="flex-1 resize-none border border-gray-300 rounded-full px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500"
          rows="1"
        />
      </div>
      <div className="flex items-center justify-between mt-3">
        <div className="flex gap-4">
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-3 py-2 rounded">
            <Image className="w-5 h-5 text-blue-500" />
            <span className="text-sm font-semibold">Photo</span>
          </button>
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-3 py-2 rounded">
            <Video className="w-5 h-5 text-green-500" />
            <span className="text-sm font-semibold">Video</span>
          </button>
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-3 py-2 rounded">
            <Calendar className="w-5 h-5 text-orange-500" />
            <span className="text-sm font-semibold">Event</span>
          </button>
        </div>
        <button
          onClick={handlePost}
          disabled={!postText.trim()}
          className="bg-blue-600 text-white px-6 py-2 rounded-full font-semibold text-sm hover:bg-blue-700 disabled:bg-gray-300 disabled:cursor-not-allowed"
        >
          Post
        </button>
      </div>
    </div>
  );

  const Post = ({ post }) => (
    <div className="bg-white rounded-lg border border-gray-200 p-4">
      <div className="flex items-start gap-3">
        <div className="w-12 h-12 rounded-full bg-gray-300 flex-shrink-0"></div>
        <div className="flex-1">
          <div className="flex justify-between items-start">
            <div>
              <h3 className="font-semibold text-sm">{post.author}</h3>
              <p className="text-xs text-gray-600">{post.title}</p>
              <p className="text-xs text-gray-500">{post.time}</p>
            </div>
            <button className="text-gray-600 hover:bg-gray-100 p-1 rounded">
              <MoreHorizontal className="w-5 h-5" />
            </button>
          </div>
          <p className="mt-3 text-sm whitespace-pre-line">{post.content}</p>
          {post.image && (
            <img src={post.image} alt="Post" className="mt-3 rounded-lg w-full" />
          )}
        </div>
      </div>
      <div className="mt-3 pt-3 border-t border-gray-200">
        <div className="flex items-center justify-between text-xs text-gray-600 mb-3">
          <span>{post.likes} likes</span>
          <span>{post.comments} comments · {post.shares} shares</span>
        </div>
        <div className="flex justify-around border-t border-gray-200 pt-2">
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-4 py-2 rounded flex-1 justify-center">
            <ThumbsUp className="w-5 h-5" />
            <span className="text-sm font-semibold">Like</span>
          </button>
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-4 py-2 rounded flex-1 justify-center">
            <MessageCircle className="w-5 h-5" />
            <span className="text-sm font-semibold">Comment</span>
          </button>
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-4 py-2 rounded flex-1 justify-center">
            <Share2 className="w-5 h-5" />
            <span className="text-sm font-semibold">Share</span>
          </button>
          <button className="flex items-center gap-2 text-gray-600 hover:bg-gray-100 px-4 py-2 rounded flex-1 justify-center">
            <Send className="w-5 h-5" />
            <span className="text-sm font-semibold">Send</span>
          </button>
        </div>
      </div>
    </div>
  );

  return (
    <div className="min-h-screen bg-gray-100">
      <NavBar />
      
      <div className="max-w-6xl mx-auto px-4 py-6">
        <div className="grid grid-cols-12 gap-6">
          <div className="col-span-3">
            <ProfileCard />
          </div>
          
          <div className="col-span-6">
            <CreatePost />
            <div className="mt-4 space-y-4">
              {posts.map(post => (
                <Post key={post.id} post={post} />
              ))}
            </div>
          </div>
          
          <div className="col-span-3">
            <div className="bg-white rounded-lg border border-gray-200 p-4">
              <h3 className="font-semibold text-sm mb-3">Add to your feed</h3>
              {[1, 2, 3].map(i => (
                <div key={i} className="flex items-start gap-2 mb-3">
                  <div className="w-10 h-10 rounded-full bg-gray-300 flex-shrink-0"></div>
                  <div className="flex-1">
                    <h4 className="text-sm font-semibold">Company Name</h4>
                    <p className="text-xs text-gray-600">Technology Industry</p>
                    <button className="mt-1 px-4 py-1 border border-gray-600 rounded-full text-xs font-semibold hover:bg-gray-100">
                      Follow
                    </button>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
